{{- if (and (eq (.Values.server.enabled | toString) "true") (eq (.Values.bootstrap.serviceAccount.create | toString) "true" )) }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "waypoint.bootstrap.serviceAccount.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    helm.sh/chart: {{ include "waypoint.chart" . }}
    app.kubernetes.io/name: {{ include "waypoint.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  {{ template "waypoint.bootstrap.serviceAccount.annotations" . }}
{{ end }}
